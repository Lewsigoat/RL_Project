# Optional: only needed when paths.*_uri points to gs://.
# Prefer workload identity; otherwise point to a local service-account file.
GOOGLE_APPLICATION_CREDENTIALS=/absolute/path/to/service-account.json
